# Código-fonte (`src`)

Esta pasta contém os módulos principais do jogo.

## Arquivos

- `jogo.py`: loop principal, eventos, atualização e renderização.
- `config.py`: constantes globais (tela, caminhos, frames, FPS).
- `funcoes.py`: funções auxiliares de regra e lógica.
- `sprites.py`: carregamento e recorte de spritesheet.
- `dados.py`: leitura e gravação de dados (recorde/ranking).
- `personagem.py`: classe dos personagens e as bibliotecas de animações.
- `telas.py`: diferentes telas do jogo e classe de botões das telas.
- `sons.py`: carregamento e agrupamento dos efeitos sonoros do jogo.
